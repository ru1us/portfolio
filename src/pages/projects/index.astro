---
import Layout from "../../layouts/Layout.astro";

const BASE_URL = import.meta.env.BASE_URL;
---

<Layout title="Projects — Overview">
  <div class="w-full mx-auto pt-8">
    <h1 class="text-4xl font-heading font-bold mb-12 text-left">
      All Projects
    </h1>

    <div class="grid grid-cols-2 gap-8 w-full">
      <!-- Project 1: FORMA Furniture Collections -->
      <div
        class="border border-slate-400 p-6 w-full h-full flex flex-col justify-start card-3d cursor-pointer hover:border-slate-500 transition-colors relative scroll-fade group"
        onclick={`window.location.href='${BASE_URL}projects/forma'`}
      >
        <!-- Project Image -->
        <div class="w-full h-48 bg-slate-200 rounded mb-4 overflow-hidden">
          <img
            src={`${BASE_URL}projects/forma/chair1.webp`}
            alt="FORMA Furniture Collections"
            class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-300"
          />
        </div>

        <div class="flex items-start justify-between mb-3">
          <h3 class="font-heading font-bold text-lg text-slate-900 flex-grow">
            FORMA — Three Furniture Collections
          </h3>
          <!-- Arrow Icon - aligned with heading -->
          <div
            class="w-5 h-5 text-red-600 hover:text-red-700 transition-colors ml-2 flex-shrink-0"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              height="20px"
              viewBox="0 -960 960 960"
              width="20px"
              fill="currentColor"
            >
              <path d="m256-240-56-56 384-384H240v-80h480v480h-80v-344L256-240Z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded"
            >3D Design</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >UI/UX</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Web Design</span
          >
        </div>

        <p class="text-sm text-slate-700 flex-grow">
          Three distinct furniture collections with unique design philosophies —
          from Mediterranean warmth to industrial precision.
        </p>
      </div>

      <!-- Project 2: KLAPP Hairdryer -->
      <div
        class="border border-slate-400 p-6 w-full h-full flex flex-col justify-start card-3d cursor-pointer hover:border-slate-500 transition-colors relative scroll-fade group"
        onclick={`window.location.href='${BASE_URL}projects/klapp-hairdryer'`}
      >
        <!-- Project Image -->
        <div class="w-full h-48 bg-slate-200 rounded mb-4 overflow-hidden">
          <img
            src={`${BASE_URL}projects/klapp/Aseite2.webp`}
            alt="KLAPP Hairdryer Industrial Design"
            class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-300"
          />
        </div>

        <div class="flex items-start justify-between mb-3">
          <h3 class="font-heading font-bold text-lg text-slate-900 flex-grow">
            KLAPP Hairdryer — Industrial Design
          </h3>
          <!-- Arrow Icon - aligned with heading -->
          <div
            class="w-5 h-5 text-red-600 hover:text-red-700 transition-colors ml-2 flex-shrink-0"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              height="20px"
              viewBox="0 -960 960 960"
              width="20px"
              fill="currentColor"
            >
              <path d="m256-240-56-56 384-384H240v-80h480v480h-80v-344L256-240Z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded"
            >Industrial Design</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >3D Modeling</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Product Design</span
          >
        </div>

        <p class="text-sm text-slate-700 flex-grow">
          Architectural clarity meets everyday functionality — a hairdryer with
          bold geometric language and reduced aesthetics.
        </p>
      </div>

      <!-- Project 3: FoodDecode UX/UI -->
      <div
        class="border border-slate-400 p-6 w-full h-full flex flex-col justify-start card-3d cursor-pointer hover:border-slate-500 transition-colors relative scroll-fade group"
        onclick={`window.location.href='${BASE_URL}projects/fooddecode-ux'`}
      >
        <!-- Project Image -->
        <div class="w-full h-48 bg-slate-200 rounded mb-4 overflow-hidden">
          <img
            src={`${BASE_URL}projects/UX/title.webp`}
            alt="UX/UI Design for FoodDecode"
            class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-300"
          />
        </div>

        <div class="flex items-start justify-between mb-3">
          <h3 class="font-heading font-bold text-lg text-slate-900 flex-grow">
            FoodDecode — UX/UI Design
          </h3>
          <!-- Arrow Icon - aligned with heading -->
          <div
            class="w-5 h-5 text-red-600 hover:text-red-700 transition-colors ml-2 flex-shrink-0"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              height="20px"
              viewBox="0 -960 960 960"
              width="20px"
              fill="currentColor"
            >
              <path d="m256-240-56-56 384-384H240v-80h480v480h-80v-344L256-240Z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded"
            >UX/UI Design</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >User Research</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Prototyping</span
          >
        </div>

        <p class="text-sm text-slate-700 flex-grow">
          Digital platform empowering healthier food choices through transparent
          ingredient analysis and intuitive UX design.
        </p>
      </div>

      <!-- Project 4: SOULSGATE -->
      <div
        class="border border-slate-400 p-6 w-full h-full flex flex-col justify-start card-3d cursor-pointer hover:border-slate-500 transition-colors relative scroll-fade group"
        onclick={`window.location.href='${BASE_URL}projects/soulsgate'`}
      >
        <!-- Project Image -->
        <div class="w-full h-48 bg-slate-200 rounded mb-4 overflow-hidden">
          <img
            src={`${BASE_URL}projects/soulsgate/hero.webp`}
            alt="SOULSGATE Cinematic Environment"
            class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-300"
          />
        </div>

        <div class="flex items-start justify-between mb-3">
          <h3 class="font-heading font-bold text-lg text-slate-900 flex-grow">
            SOULSGATE — Cinematic Environment
          </h3>
          <!-- Arrow Icon - aligned with heading -->
          <div
            class="w-5 h-5 text-red-600 hover:text-red-700 transition-colors ml-2 flex-shrink-0"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              height="20px"
              viewBox="0 -960 960 960"
              width="20px"
              fill="currentColor"
            >
              <path d="m256-240-56-56 384-384H240v-80h480v480h-80v-344L256-240Z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded"
            >3D Design</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Environment Art</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Animation</span
          >
        </div>

        <p class="text-sm text-slate-700 flex-grow">
          Dark atmospheric graveyard environment with cinematic flythrough —
          from concept sketches to final 3D animation.
        </p>
      </div>

      <!-- Project 5: Motion Design -->
      <div
        class="border border-slate-400 p-6 w-full h-full flex flex-col justify-start card-3d cursor-pointer hover:border-slate-500 transition-colors relative scroll-fade group"
        onclick={`window.location.href='${BASE_URL}projects/motion-design'`}
      >
        <!-- Project Image -->
        <div class="w-full h-48 bg-slate-200 rounded mb-4 overflow-hidden">
          <img
            src={`${BASE_URL}projects/motion/title.png`}
            alt="Motion Design Collection"
            class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-300"
          />
        </div>

        <div class="flex items-start justify-between mb-3">
          <h3 class="font-heading font-bold text-lg text-slate-900 flex-grow">
            Motion Design — 7 Projects
          </h3>
          <!-- Arrow Icon - aligned with heading -->
          <div
            class="w-5 h-5 text-red-600 hover:text-red-700 transition-colors ml-2 flex-shrink-0"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              height="20px"
              viewBox="0 -960 960 960"
              width="20px"
              fill="currentColor"
            >
              <path d="m256-240-56-56 384-384H240v-80h480v480h-80v-344L256-240Z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded"
            >Motion Design</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Animation</span
          >
          <span class="bg-slate-100 text-slate-600 text-xs px-2 py-1 rounded"
            >Video</span
          >
        </div>

        <p class="text-sm text-slate-700 flex-grow">
          A collection of motion design projects showcasing various animation styles and techniques.
        </p>
      </div>
    </div>
  </div>
</Layout>

<script>
  // Card Tilt Effect for Project Cards
  document.addEventListener("DOMContentLoaded", () => {
    const cards = document.querySelectorAll(".card-3d");

    cards.forEach((card) => {
      const htmlCard = card as HTMLElement;

      htmlCard.addEventListener("mouseenter", () => {
        htmlCard.style.transition = "transform 0.1s ease-out";
      });

      htmlCard.addEventListener("mousemove", (e: MouseEvent) => {
        const rect = htmlCard.getBoundingClientRect();
        const relativeX = e.clientX - rect.left;
        const relativeY = e.clientY - rect.top;
        const xPercent = relativeX / rect.width;
        const yPercent = relativeY / rect.height;

        const tiltX = (yPercent - 0.5) * 5;
        const tiltY = (0.5 - xPercent) * 5;

        htmlCard.style.transform = `perspective(700px) rotateX(${tiltX}deg) rotateY(${tiltY}deg) translateZ(10px)`;
      });

      htmlCard.addEventListener("mouseleave", () => {
        htmlCard.style.transition = "transform 0.1s ease-out";
        htmlCard.style.transform =
          "perspective(700px) rotateX(0deg) rotateY(0deg) translateZ(0px)";
      });
    });
  });
</script>
